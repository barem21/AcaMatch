interface HeaderProps {
  className?: string;
}
const Header: React.FC<HeaderProps> = ({ className }) => {
  return (
    <>
      <header className={className}>asda</header>
    </>
  );
};

export default Header;
